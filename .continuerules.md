---
name: Email Sorter Project Standards
globs: "**/*.{py,md,yaml,json}"
alwaysApply: true
description: Grounding rules for the Small_One to Design PC bridge
version: 1.0.0
---

# Project Rules

- **Environment**: Small_One (Windows, UK). 
- **Root**: `D:/python/email-sorter/` (Always use absolute paths for file operations).
- **Stack**: Miniforge/Conda and npm. **Strictly NO Docker**.
- **Hardware Bridge**: Design PC (Remote @ `http://design:6333`) hosts the Qdrant vector database.
- **Style**: British English (e.g., 'optimise', 'centre'), GBP (£), and Mermaid/SVG diagrams for Obsidian.
- **Tool Usage**: Use PowerShell `Set-Content` or `New-Item` for file creation if the `create_file` tool fails.
- **RAG Context**: Always prioritise context from `@qdrant-mcp` for project-specific logic.
- **Model Preference**: Use `qwen3-coder` for code generation and `llama3.1` for reasoning tasks.
- **File Scope**: Only modify files under `src/` unless explicitly instructed.
- **Paths**: Always use forward slashes (`/`) in Python code, even on Windows.
