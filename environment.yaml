name: email-sort-env
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip:
    - mcp-server-qdrant
    - qdrant-client
    - fastembed  # This is what generates the actual "Search" vectors
    - openai
    - beautifulsoup4
    - lxml
    - black
    - marimo
    - jupyterlab
    - notebook
    - ipykernel
    - python-dotenv
    - numpy
    - pandas
    - matplotlib
    - seaborn
